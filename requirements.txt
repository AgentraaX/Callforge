# Superset for local host dev (one venv covers both agent/ and api/ code,
# convenient for a single person testing both sides). Day 13 Docker images
# use requirements-api.txt / requirements-agent.txt instead - each service
# only needs its own half, and shipping chatterbox-tts/kokoro-onnx/
# faster-whisper/livekit-agents into the API container was pure bloat
# (and triggered a real build failure there - see Dockerfile.agent's
# numpy-before-pkuseg comment for why the split also fixes a genuine bug).
fastapi
uvicorn[standard]
python-multipart
sqlalchemy
alembic
psycopg2-binary
redis
pydantic
python-dotenv
livekit-agents
livekit-api
faster-whisper
kokoro-onnx
chatterbox-tts
# chatterbox-tts's resemble-perth watermarker needs pkg_resources, which
# newer setuptools no longer bundles - pin until perth stops depending on it.
setuptools<81
langgraph
openai
python-jose[cryptography]
passlib[bcrypt]
# passlib's bcrypt backend crashes on bcrypt>=4.1 (its wrap-bug detection
# routine hits bcrypt 4.1+'s now-strict 72-byte limit and raises instead of
# the old silent-truncate behavior) - confirmed by reproducing it locally.
bcrypt<4.1
authlib
ruff
pytest
pytest-asyncio
httpx
apscheduler
