fastapi
uvicorn[standard]
python-multipart
sqlalchemy
alembic
psycopg2-binary
redis
pydantic
# pydantic's EmailStr (used by api/schemas/contact.py) needs this extra.
email-validator
python-dotenv
apscheduler
httpx
python-jose[cryptography]
passlib[bcrypt]
# passlib's bcrypt backend crashes on bcrypt>=4.1 (its wrap-bug detection
# routine hits bcrypt 4.1+'s now-strict 72-byte limit and raises instead of
# the old silent-truncate behavior) - confirmed by reproducing it locally.
bcrypt<4.1
authlib
# For POST /calls/{id}/monitor (manager listen-in) - api/services/livekit_rooms.py
livekit-api
# authlib's requests_client integration (used for the Google/GitHub OAuth
# code exchange in api/services/auth.py) imports `requests` directly but
# doesn't declare it as its own dependency - without this, `api.main`
# fails to import at all.
requests
ruff
pytest
pytest-asyncio
